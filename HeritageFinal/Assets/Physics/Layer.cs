using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Layer : MonoBehaviour {
    /*
        Name: Layer
        Type: Global Script
        Purpose: Different global constants to act as shorthand for different layers,
            just in case the numbering of the layers change
    */


    public const int PLAYER = 1 << 8;

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
